[English](./USER_MANUAL.md)

# Life Dashboard — 用户使用手册

按左侧导航栏的顺序，逐个介绍面板里的每一页。建议你**一边打开应用一边阅读本手册**。

如果你还没安装好面板，先看 **[INSTALL.zh-CN.md](./INSTALL.zh-CN.md)**。

---

## 整体布局

打开 `http://localhost:3000` 后，你会进入仪表盘首页。整个界面有三个常驻区域：

- **左侧导航栏** —— 顶级导航。点击底部的箭头可以折叠成图标。"Myself" 这一组（思维导图、待办、财务、计划、发票、保险箱、收藏）会在你浏览其中一页时自动展开。
- **顶部头部栏** —— 显示当前页面标题以及全局控件（主题切换、语言切换等）。
- **主区域** —— 实际页面内容。

所有数据都保存在本机的 `data/dashboard.db` 与 `data/vault.db` 两个文件里，**不会上传任何数据**。

---

## 仪表盘首页

**是什么。** 一种"游戏式"的总览页面，把几个常用控件聚合在一起，让你一眼看清今天的状态。

**你能看到什么。**

- 顶部的 **心愿清单**，快速记录你想要的东西。
- **用户面板** 配有一个 2D 像素角色——每添加一个技能，都会为角色装备一件相应的配件（编程类显示眼镜、设计类显示调色板、音乐类显示耳机、运动类显示佩剑、商务类显示公文包，其他显示徽章）。技能等级决定配件颜色（灰→蓝→金），脚下的圆点串记录技能总数。
- **待办预览**：来自待办页的下一批未完成事项。
- **Claw 面板**：内置 AI 助手的小型聊天窗口。其像素龙虾会根据每个已安装技能的 `metadata.type`（或名称/描述中的关键词匹配）装备相应配件；新增技能时播放闪光动画，删除时播放消散动画。

**小贴士。**

- 两个像素角色都保留了原有的取色器——点击面板上的调色盘图标即可调整角色（皮肤/头发/服装）或龙虾（壳色/腹色/眼色）的配色。
- 在心愿清单或 Claw 面板里添加的内容，会自动同步到对应的完整页面 —— 底层数据库只有一份。

---

## 思维导图（Mind Map）

**是什么。** 一张自由的人生可视化地图。你可以创建**分类**（健康、职业、财务、关系等），在分类下添加**条目**，再随意拖拽连线。

**核心操作。**

- 在右侧面板里添加分类或条目。
- 在画布上拖动节点；从节点边缘往外拖动来连线。
- 顶部模式切换可以在三种模式之间切换：自由的 **思维导图画布**、**Excalidraw 画布**（手绘风格）、以及 **产品画布**（包含需求 / 功能 / 利益相关者 / 用户面板的产品发现布局，对应 `components/mind-map/panels/`）。
- 添加条目时，会自动在 **待办** 页生成对应任务。

**小贴士。**

- 大胆使用分类，分类同时也是其他页面的过滤维度。
- Excalidraw 模式适合快速白板涂鸦；产品画布则适合把一个项目当成可发现的整体来思考。

---

## 待办（Todos）

**是什么。** 一个简洁的清单页，主要由思维导图驱动。也可以直接在这里手动添加。

**核心操作。**

- 点击复选框标记完成。
- 顶部输入框新增待办。
- 用垃圾桶图标删除条目。
- 单击文字可以原地编辑标题。

**小贴士。**

- 仪表盘首页只显示这里的"下一批"未完成项。把列表保持整洁，首页就一直好用。

---

## 财务（Finance）

**是什么。** 个人理财：账户、收支、预算，以及手动维护的投资持仓。数据与其他功能一样按用户存在数据库中。

**核心操作。**

- **概览** Tab —— 净资产摘要、现金流图、支出分类、账户列表。
- **交易** Tab —— 添加与筛选收入、支出、转账。
- **预算** Tab —— 按分类设置月度上限。
- **投资** Tab —— 按代码维护持仓（手动录入）。

**小贴士。**

- 添加交易前请先创建至少一个账户。
- 交易上的分类会驱动预算与支出图表。

---

## 计划（Plans）

**是什么。** 一个富文本笔记本，支持多页。每页都是一份 [BlockNote](https://www.blocknotejs.org/) 文档，支持标题、列表、代码块、表格、嵌入和"斜杠命令"。

**核心操作。**

- 左侧 **页面列表** 用于新增、重命名、排序、删除页面。
- 在编辑器里输入 `/` 打开块菜单（标题、列表、表格、图片等）。
- 顶部的 **导出/导入工具栏** 可将当前页导出为 **DOCX**（通过 `@blocknote/xl-docx-exporter`）或 **PDF**（通过 `jspdf` + `html2canvas`），也可使用 `mammoth` 导入 DOCX。
- 内嵌的小型 **Claw** 入口让你在写作时随时召唤助手。

**小贴士。**

- 长文笔记建议放计划页；待办放在 Todos；零碎念头放在心愿清单。

---

## 发票（Invoice）

**是什么。** 一个面向自由职业者的全功能发票工具：管理客户、生成发票、签字、导出 PDF。

**核心操作。**

- **客户管理** —— 添加/编辑客户（名称、邮箱、地址、默认税率/币种）。
- **发票编辑器** —— 行项、数量、单价、税额、备注、到期日。
- **发票列表** —— 按状态筛选（草稿、已发送、已支付）。
- **发票预览** —— 查看导出前的真实呈现效果。
- **签名管理** —— 使用触摸板/鼠标手写签名（基于 `signature_pad`），保存后可在所有发票复用。
- 在预览页一键导出 PDF。

**小贴士。**

- 在 **设置 → 发票** 中先把公司名称、Logo、默认税率、银行信息等填好，每张新发票就会自动带上。

---

## 保险箱（Vault）

**是什么。** 一个加密的本地密钥/敏感信息仓库。可以理解为住在 `data/vault.db` 里的一份离线版 Bitwarden。

**核心操作。**

- **首次使用：** 设置主密码，激活保险箱。请像对待真正的密码那样保管它 —— **设计上不可恢复**。
- **解锁：** 每次进入页面（或自动锁定超时后）输入主密码。
- **添加密钥** —— 标题、用户名、密码、URL、备注。
- **密钥列表** —— 搜索、一键复制到剪贴板、编辑、删除。
- **保险箱预览** —— 嵌在其他页面里的只读小窗口。
- **保险箱设置** —— 修改主密码、设置自动锁定时长、清空保险箱。

**加密原理（通俗说明）。** 你设置主密码后，应用通过 **scrypt** 派生出加密密钥；你的密钥使用 **XChaCha20-Poly1305**（一种现代认证加密算法）加密；同时存储派生密钥的 **SHA3-256** 哈希以便校验密码是否正确，**主密码本身从不落盘**。代码在 `lib/modules/vault/crypto.ts`。这意味着：

- `data/vault.db` 文件即便被人拿到，没有主密码也读不出来。
- 一旦主密码丢失，里面的内容就**无法找回**。担心的话请把数据库文件备份到安全的位置。

---

## 收藏（Marked）

**是什么。** 一个书签/收藏管理器。保存链接、片段或图片，按集合归类，并能导出可分享的精美卡片。

**核心操作。**

- **集合列表** —— 添加、重命名、删除集合。
- **添加条目** —— 标题、URL、描述、配图、标签。
- **条目卡片** —— 打开来源链接、编辑、复制、删除。
- **分享卡片预览/渲染** —— 为任意条目生成排版精美的图片卡片，方便发到社交媒体（底层使用 `html-to-image`）。

**小贴士。**

- 那种"我以后想再翻出来看"的链接和片段就放进收藏，不必塞进待办或计划。

---

## Claw（AI 助手）

**是什么。** 面板自带的智能体。可以与你聊天、调用工具读写面板数据（待办、财务、计划、设置等）、运行命令行工具、并提供独立的管理面板。

Claw 页面分为基础的 **DM 聊天面板** 和功能更全面的 **高级视图**。

**DM 聊天面板。**

- **智能输入框**：模板弹窗、分类标签、聚光灯快速短语。
- **消息流**：支持 Markdown 与生成式回复卡片。
- **会话列表**：开新会话或回到旧会话。
- **工具审批卡片**：当 Claw 想调用会改写你数据的工具（比如新建待办）时，会先弹出一键审批。
- **定时任务面板**：让某些提示按计划循环触发。
- **快捷指令栏**：你保存的一次性 Prompt。

**高级视图子面板。**

- **状态面板** —— 显示助手是否在线、使用的是哪个模型。
- **记忆面板** —— 助手长期记住的关于你的事实。
- **会话面板** —— 完整的会话历史，可重命名/删除/置顶。
- **频道面板** —— 类似 Slack 的会话频道（例如每个项目一个）。
- **技能市场 + 技能编辑器** —— 安装或手写技能（小型可复用能力）。
- **灵魂编辑器** —— 编辑助手人格 / 系统提示词。
- **CLI 工具面板** —— 注册允许助手调用的外部命令行工具。
- **公共 API 面板** —— 注册允许助手调用的 HTTP 接口。
- **文件面板** —— 当前会话挂载的文件。
- **代码编辑器** + **JSON 表单编辑器** —— 直接修改技能代码与配置。
- **配置编辑器** —— 连接设置（模型、API Key 端点）。
- **连接表单** —— 在不同服务商之间快速切换。
- **网关控制** —— 启用/禁用本地代理网关。
- **日志查看器** —— 调试助手做了什么、什么时候做的。
- **终端** —— 内嵌的 `xterm.js` 终端，让助手能看到真实命令行。

**先配 API Key。** 进入 **设置 → Claw 访问**，粘贴你的 OpenRouter API Key（面板通过 OpenAI SDK 与 OpenRouter 通讯）。在此之前，所有 Claw 回复都会因鉴权失败而出错。

**小贴士。**

- 平时聊天就用 DM 面板。要安装技能或挂载 CLI 工具时再进高级视图。
- 工具调用默认走审批流程。如果信任某些工具，可在 Claw 访问设置里调高它们的信任级别，让它们自动执行。

---

## 设置（Settings）

设置页分若干 Tab：

- **外观** —— 浅色/深色/跟随系统、语言（English / 简体中文）、强调色。
- **发票** —— 你的公司名称、Logo、地址、税率、币种、收款方式。每张新发票会自动带上。
- **Claw 访问** —— OpenRouter API Key、模型选择、各工具的默认审批策略。
- **数据管理** —— 一键导出全部数据为 JSON、再导入回来，或者清空数据库。

**小贴士。**

- 面板**不读取 `.env` 文件**，所有原本会写在环境变量里的配置都集中在这里。
- 修改 OpenRouter Key 后，**无需重启**，立即生效。

---

## 数据存放在哪里

所有数据都在你本机的两个 SQLite 文件里：

| 文件 | 内容 |
|------|------|
| `data/dashboard.db` | 思维导图、待办、计划、发票、收藏、设置、财务、Claw |
| `data/vault.db`     | 仅保险箱加密数据 |

**备份**：复制这两个文件（以及它们的 `-shm` / `-wal` 配套文件）到安全位置即可。

**清空一切**：先在终端按 `Ctrl+C` 停止面板，删除 `data/` 文件夹，再次运行 `npm run dev`，会自动新建空数据库。

**迁移到新电脑**：把整个项目文件夹（包含 `data/`）复制过去，运行 `npm install && npm run dev`。

---

## 高阶用户 —— OpenClaw 与 Supabase

Wiki 同步与 wiki 模式的 Claw 聊天会在 **SSH 消息** 内向 openclaw 提供 Supabase 连接信息：URL 与 anon key 来自服务端环境变量，**refresh token** 请在 **面板 → 设置 → OpenClaw / wiki ingest** 保存（可使用「从当前会话获取」或手动粘贴），服务端使用 **`MYSELF_OPENCLAW_TOKEN_KEY`** 加密后写入数据库。

在运行 openclaw 的机器上，按消息中的分隔块导出 `SUPABASE_URL`、`SUPABASE_ANON_KEY`、`SUPABASE_REFRESH_TOKEN`，再使用仓库 **`openclaw/skills/supabase-reads/`**（见 `SKILL.md` 与 `scripts/`）。数据访问受 RLS 约束，等同于当前登录用户。切勿在 SSH 主机使用 **service-role** 密钥。

更深入的说明见 [README.zh-CN.md](./README.zh-CN.md)。

---

## 求助

- 环境/安装相关问题，回看 [INSTALL.zh-CN.md](./INSTALL.zh-CN.md)。
- 技术栈与架构详情，见 [README.zh-CN.md](./README.zh-CN.md)。
- 本手册没覆盖的问题，欢迎在 GitHub 提 Issue。
